# qwen3-asr-service
Self-hosted ASR inference service powered by **FastAPI** + **vLLM**, with **OpenAI-compatible API endpoints**. Deploy locally, call like cloud — no API keys, no rate limits, no cost per minute. 
